# RecoverAI Development Scripts

This directory contains reproducible project utility scripts.

## generate_demo_data.py

Generates deterministic synthetic transaction and recovery scenario
data for development and presentation.

Run from the repository root:

```powershell
python .\scripts\generate_demo_data.py
